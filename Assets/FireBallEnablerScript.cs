using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FireBallEnablerScript : MonoBehaviour
{
    // Start is called before the first frame update
    public bool FireEnablerBool;
    void Start()
    {
        FireEnablerBool = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
